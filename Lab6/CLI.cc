#include "FSAPI.h"

int main (int argc, char *argv[]) {
    // printf("%ld\n", sizeof(i_node));
    // i_bitmap[0] = 9;
    // printf("%d\n", i_bitmap[0]);
    openFp();
    printf("%d %d\n", i_bitmap[0], b_bitmap[1]);
    // i_bitmap[0] = 9;
    // b_bitmap[1] = 10;
    // writeThrough();
    // initSpace();
}